This fork contains a custom build of WiX used by the [Velopack](https://velopack.io)
cross-platform install and update framework.

The source contained here is MS-RL and is available freely with no restrictions.

To build `wixc`, the binary used by Velopack and all it's dependencies, 
the `./wixc/build.bat` script is all that need be run.
